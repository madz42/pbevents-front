export const selectBunkers = (state) => state.build.bunkers;
export const selectSet = (state) => state.build.set;
export const selCurrentBunker = (state) => state.build.selected;
export const selectPOV = (state) => state.build.pov;
